import 'package:flutter/material.dart';

class Progeram extends StatelessWidget {
  const Progeram({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Progeram"),);
  }
}